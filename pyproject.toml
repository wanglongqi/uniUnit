[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "uniunit"
version = "1.0.3"
description = "Consistent units manager for Python"
readme = "README.md"
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "pint",
]

[project.urls]
Homepage = "https://github.com/wanglongqi/uniUnit"
Repository = "https://github.com/wanglongqi/uniUnit"

[tool.setuptools.packages.find]
where = ["."]
include = ["uniunit*"]
